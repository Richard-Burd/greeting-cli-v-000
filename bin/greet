#!/usr/bin/env ruby
require_relative "../lib/greeting.rb"

# code your CLI here!
puts "Hi! I'm HAL, what's your name?"
  if gets.is_an_int
    name = gets.to_i
  else
    name = gets.strip
  end
greeting(name)
